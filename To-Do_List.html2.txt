<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do Task List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f5f5f5;
            transition: background-color 0.3s, color 0.3s;
        }
        .dark-mode {
            background-color: #333;
            color: white;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 400px;
            transition: background-color 0.3s, box-shadow 0.3s;
        }
        .dark-mode .container {
            background: #444;
            box-shadow: 0px 4px 10px rgba(255, 255, 255, 0.2);
        }
        h2 {
            margin-bottom: 10px;
        }
        input {
            padding: 8px;
            width: 60%;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            padding: 8px 12px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            margin: 5px;
            transition: all 0.3s ease-in-out;
        }
        .add-btn {
            background: green;
            color: white;
            box-shadow: 0px 3px 5px rgba(0, 128, 0, 0.3);
        }
        .add-btn:hover {
            background: darkgreen;
            box-shadow: 0px 5px 8px rgba(0, 100, 0, 0.5);
        }
        .clear-btn {
            background: brown;
            color: white;
            box-shadow: 0px 3px 5px rgba(139, 69, 19, 0.3);
        }
        .clear-btn:hover {
            background: darkred;
            box-shadow: 0px 5px 8px rgba(139, 0, 0, 0.5);
        }
        .filter button {
            color: white;
        }
        .filter button:nth-child(1) {
            background-color: blue;
            box-shadow: 0px 3px 5px rgba(0, 0, 255, 0.3);
        }
        .filter button:nth-child(1):hover {
            background-color: darkblue;
            box-shadow: 0px 5px 8px rgba(0, 0, 150, 0.5);
        }
        .filter button:nth-child(2) {
            background-color: orange;
            box-shadow: 0px 3px 5px rgba(255, 165, 0, 0.3);
        }
        .filter button:nth-child(2):hover {
            background-color: darkorange;
            box-shadow: 0px 5px 8px rgba(255, 100, 0, 0.5);
        }
        .filter button:nth-child(3) {
            background-color: green;
            box-shadow: 0px 3px 5px rgba(0, 128, 0, 0.3);
        }
        .filter button:nth-child(3):hover {
            background-color: darkgreen;
            box-shadow: 0px 5px 8px rgba(0, 100, 0, 0.5);
        }
        .task {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #ddd;
            padding: 10px;
            margin-top: 10px;
            border-radius: 5px;
            cursor: pointer;
        }
        .completed {
            background: orange !important;
            color: white;
        }
        .completed::after {
            content: ' ✔';
            color: white;
            font-weight: bold;
        }
        .edit-btn {
            background: #fdf5e6;
            box-shadow: 0px 3px 5px rgba(255, 200, 100, 0.3);
        }
        .edit-btn:hover {
            background: #ffcc80;
            box-shadow: 0px 5px 8px rgba(255, 150, 50, 0.5);
        }
        .delete-btn {
            background: red;
            color: white;
            box-shadow: 0px 3px 5px rgba(255, 0, 0, 0.3);
        }
        .delete-btn:hover {
            background: darkred;
            box-shadow: 0px 5px 8px rgba(200, 0, 0, 0.5);
        }
        .counter {
            margin-top: 10px;
            font-weight: bold;
        }
        .dark-mode-toggle {
            margin-top: 20px;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            background-color: #222;
            color: white;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Task List</h2>
        <input type="text" id="taskInput" placeholder="Enter task">
        <button class="add-btn" onclick="addTask()">Add Task</button>
        <button class="clear-btn" onclick="clearTasks()">Clear All</button>

        <div class="counter">
            <p>Pending: <span id="pendingCount">0</span> | Completed: <span id="completedCount">0</span></p>
        </div>

        <div class="filter">
            <button onclick="filterTasks('all')">All</button>
            <button onclick="filterTasks('pending')">Pending</button>
            <button onclick="filterTasks('completed')">Completed</button>
        </div>

        <div id="taskList"></div>

        <!-- Dark Mode Toggle Button -->
        <button class="dark-mode-toggle" onclick="toggleDarkMode()">Toggle Dark Mode</button>
    </div>

    <script>
        let isDarkMode = false;

        document.addEventListener("DOMContentLoaded", () => {
            loadTasks();

            document.getElementById("taskInput").addEventListener("keypress", function(event) {
                if (event.key === "Enter") {
                    addTask();
                }
            });
        });

        function addTask() {
            let taskInput = document.getElementById("taskInput");
            let taskText = taskInput.value.trim();
            if (taskText === "") return;

            let task = { text: taskText, completed: false };
            let tasks = JSON.parse(localStorage.getItem("tasks")) || [];
            tasks.push(task);
            localStorage.setItem("tasks", JSON.stringify(tasks));

            taskInput.value = "";
            loadTasks();
        }

        function loadTasks(filter = "all") {
            let taskList = document.getElementById("taskList");
            taskList.innerHTML = "";
            let tasks = JSON.parse(localStorage.getItem("tasks")) || [];

            let pendingCount = 0;
            let completedCount = 0;

            tasks.forEach((task, index) => {
                if (filter === "pending" && task.completed) return;
                if (filter === "completed" && !task.completed) return;

                let taskDiv = document.createElement("div");
                taskDiv.classList.add("task");
                if (task.completed) taskDiv.classList.add("completed");

                taskDiv.innerHTML = `
                    <span ondblclick="editTask(${index})">${task.text}</span>
                    <div>
                        <button class="edit-btn" onclick="editTask(${index})">Edit</button>
                        <button class="delete-btn" onclick="deleteTask(${index})">Delete</button>
                    </div>`;

                taskDiv.addEventListener("click", () => toggleComplete(index));
                taskList.appendChild(taskDiv);

                task.completed ? completedCount++ : pendingCount++;
            });

            document.getElementById("pendingCount").textContent = pendingCount;
            document.getElementById("completedCount").textContent = completedCount;
        }

        function deleteTask(index) {
            let tasks = JSON.parse(localStorage.getItem("tasks")) || [];
            tasks.splice(index, 1);
            localStorage.setItem("tasks", JSON.stringify(tasks));
            loadTasks();
        }

        function toggleComplete(index) {
            let tasks = JSON.parse(localStorage.getItem("tasks")) || [];
            tasks[index].completed = !tasks[index].completed;
            localStorage.setItem("tasks", JSON.stringify(tasks));
            loadTasks();
        }

        function clearTasks() {
            localStorage.removeItem("tasks");
            loadTasks();
        }

        function filterTasks(filter) {
            loadTasks(filter);
        }

        function editTask(index) {
            let tasks = JSON.parse(localStorage.getItem("tasks")) || [];
            let newText = prompt("Edit task:", tasks[index].text);
            if (newText !== null && newText.trim() !== "") {
                tasks[index].text = newText.trim();
                localStorage.setItem("tasks", JSON.stringify(tasks));
                loadTasks();
            }
        }

        function toggleDarkMode() {
            isDarkMode = !isDarkMode;
            document.body.classList.toggle("dark-mode", isDarkMode);
        }
    </script>
</body>
</html>
